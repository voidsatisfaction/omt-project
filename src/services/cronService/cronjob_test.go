package cronService

import (
	"testing"
)

func TestCheckTimerAndPushQuizUrl(t *testing.T) {
	// testTime := time.Date(2009, time.November, 10, 12, 34, 0, 0, time.UTC)
	// checkTimerAndPushQuizUrl(testTime)()
}
